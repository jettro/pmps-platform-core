[tool.uv.workspace]
members = [
    "packages/core-domain",
    "packages/core-services"
]

# This block is managed by scripts/select-sources.py. Release is the canonical
# committed mode; use make dev-framework to opt in to editable framework source.
# BEGIN GENERATED DEVELOPMENT SOURCES
[tool.uv.sources]
core-domain = { workspace = true }
core-services = { workspace = true }
framework-core = { index = "local" }
framework-infra = { index = "local" }
# END GENERATED DEVELOPMENT SOURCES

[[tool.uv.index]]
name     = "local"
url      = "http://localhost:8080/simple/"
explicit = true

[tool.pytest.ini_options]
testpaths = ["packages"]
